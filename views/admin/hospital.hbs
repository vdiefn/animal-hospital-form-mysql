<div class="container">
  <div class="row mb-3">
    <div class="col-md-12">
      <h1>{{hospital.name}}<span style="font-size:36px;">&ensp;•&ensp;後台</span></h1>
      <h5>{{hospital.Location.name}}</h5>
    </div>
    <div class="col-md-5">
      <div style="">
        <img class="img-responsive center-block mb-2" src="{{hospital.image}}" width="100%"
         onload="this.style.opacity=1;">
      </div>
      <div class="well mt-3">
        <ul class="list-unstyled mb-1">
          
          <li><strong>電話:</strong> {{hospital.telephone}}</li>
          <li><strong>地址:</strong> {{hospital.address}}</li>
          <li><strong>營業時間:</strong> {{hospital.openingHours}} - {{hospital.closingHours}}</li>
          <li><strong>是否提供急診服務:</strong></li>
          <li><strong>網站：</strong> <a href="{{hospital.website}}" target="_blank">{{hospital.website}}</a></li>
        </ul>
      </div>
    </div>
    <div class="col-md-7">
      <p style="white-space: break-spaces; word-wrap: break-word">{{hospital.description}}</p>
    </div>
    <hr>
  </div>
  <div class="d-flex mb-3">
    <div class="me-auto p-2">
      <a class="btn btn-outline-success" href="/admin/hospitals/{{ hospital.id }}/edit">修改</a>
      <a class="btn btn-outline-secondary" href="javascript:history.back()">Back</a>
    </div>
    <div class="p-2">
      <form action="/admin/hospitals/{{this.id}}?_method=DELETE" method="POST" style="display: inline;">
        <button type="submit" class="btn btn-outline-danger link-danger" style="text-decoration:none;"
          onclick="return confirm(`確定要刪除 {{ this.name }} 嗎？`)">Delete</button>
      </form>
    </div>
  </div>

</div>